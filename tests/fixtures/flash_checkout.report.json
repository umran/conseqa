{
  "format": 7,
  "dsl": 4,
  "model_revision": 1,
  "obligations": [
    {
      "id": "oblig.operation.apply_payment.tx.apply_payment.transaction_serializability.0",
      "property": {
        "kind": "transaction_serializability"
      },
      "subject": {
        "kind": "transaction",
        "operation": "operation.apply_payment",
        "transaction": "tx.apply_payment",
        "requirement": 0
      },
      "status": "proven",
      "summary": "Executions of tx.apply_payment sharing key input.apply_payment.captured.order_id commit in a history equivalent to some serial order, together with every transaction they may conflict with.",
      "scope": "l0_only",
      "assumptions": [
        "the conflict closure of tx.apply_payment under SerializableBy(input.apply_payment.captured.order_id) is {tx.apply_payment, tx.cancel_order, tx.create_order.new}: every transaction that may touch overlapping state, transitively",
        "the read-write anti-dependency from `tx.apply_payment` (step 1) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.apply_payment` (step 1) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.apply_payment` (step 2) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the write-read from `tx.apply_payment` (step 3) to `tx.apply_payment` (step 1) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.apply_payment` (step 3) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered by the cursor `object.order.last_applied_sequence` (successor): an older accepted position cannot commit after a newer one",
        "the read-write anti-dependency from `tx.apply_payment` (step 3) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered by the cursor `object.order.last_applied_sequence` (successor): an older accepted position cannot commit after a newer one",
        "the write-write from `tx.apply_payment` (step 3) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered by the cursor `object.order.last_applied_sequence` (successor): an older accepted position cannot commit after a newer one",
        "the read-write anti-dependency from `tx.apply_payment` (step 4) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the write-read from `tx.apply_payment` (step 4) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.apply_payment` (step 4) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-read from `tx.apply_payment` (step 5) to `tx.apply_payment` (step 1) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.apply_payment` (step 5) to `tx.apply_payment` (step 2) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.apply_payment` (step 5) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the read-write anti-dependency from `tx.apply_payment` (step 1) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.apply_payment` (step 2) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.apply_payment` (step 4) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the write-read from `tx.apply_payment` (step 4) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.apply_payment` (step 4) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-read from `tx.apply_payment` (step 5) to `tx.cancel_order` (step 1) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.apply_payment` (step 5) to `tx.cancel_order` (step 2) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.apply_payment` (step 5) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the read-write anti-dependency from `tx.apply_payment` (step 1) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.apply_payment` (step 2) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.apply_payment` (step 3) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the write-write from `tx.apply_payment` (step 3) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the read-write anti-dependency from `tx.apply_payment` (step 4) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the write-write from `tx.apply_payment` (step 4) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-write from `tx.apply_payment` (step 5) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the read-write anti-dependency from `tx.cancel_order` (step 1) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.cancel_order` (step 2) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.cancel_order` (step 3) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the write-read from `tx.cancel_order` (step 3) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.cancel_order` (step 3) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-read from `tx.cancel_order` (step 4) to `tx.apply_payment` (step 1) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.cancel_order` (step 4) to `tx.apply_payment` (step 2) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.cancel_order` (step 4) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the read-write anti-dependency from `tx.cancel_order` (step 1) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.cancel_order` (step 2) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.cancel_order` (step 3) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the write-read from `tx.cancel_order` (step 3) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.cancel_order` (step 3) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-read from `tx.cancel_order` (step 4) to `tx.cancel_order` (step 1) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.cancel_order` (step 4) to `tx.cancel_order` (step 2) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.cancel_order` (step 4) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the read-write anti-dependency from `tx.cancel_order` (step 1) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.cancel_order` (step 2) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the read-write anti-dependency from `tx.cancel_order` (step 3) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "the write-write from `tx.cancel_order` (step 3) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-write from `tx.cancel_order` (step 4) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-read from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 1) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 2) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-read from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-write from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-read from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 1) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 2) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-read from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "the write-write from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-write from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "the write-write from `tx.create_order.new` (step 1) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "no cyclic conflict component contains an unconstrained dependency: an apparent cycle would imply a cycle in strict commit order and cannot occur in a committed history"
      ],
      "evidence": []
    },
    {
      "id": "oblig.operation.apply_payment.tx.apply_payment.transaction_ordering.0",
      "property": {
        "kind": "transaction_ordering"
      },
      "subject": {
        "kind": "transaction",
        "operation": "operation.apply_payment",
        "transaction": "tx.apply_payment",
        "requirement": 0
      },
      "status": "proven",
      "summary": "Executions of tx.apply_payment sharing key input.apply_payment.captured.order_id take effect in the order of input.apply_payment.captured.sequence.",
      "scope": "l0_only",
      "assumptions": [
        "transaction state history is serializable:",
        "  the conflict closure of tx.apply_payment under SerializableBy(input.apply_payment.captured.order_id) is {tx.apply_payment, tx.cancel_order, tx.create_order.new}: every transaction that may touch overlapping state, transitively",
        "  the read-write anti-dependency from `tx.apply_payment` (step 1) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.apply_payment` (step 1) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.apply_payment` (step 2) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the write-read from `tx.apply_payment` (step 3) to `tx.apply_payment` (step 1) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.apply_payment` (step 3) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered by the cursor `object.order.last_applied_sequence` (successor): an older accepted position cannot commit after a newer one",
        "  the read-write anti-dependency from `tx.apply_payment` (step 3) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered by the cursor `object.order.last_applied_sequence` (successor): an older accepted position cannot commit after a newer one",
        "  the write-write from `tx.apply_payment` (step 3) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered by the cursor `object.order.last_applied_sequence` (successor): an older accepted position cannot commit after a newer one",
        "  the read-write anti-dependency from `tx.apply_payment` (step 4) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the write-read from `tx.apply_payment` (step 4) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.apply_payment` (step 4) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-read from `tx.apply_payment` (step 5) to `tx.apply_payment` (step 1) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.apply_payment` (step 5) to `tx.apply_payment` (step 2) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.apply_payment` (step 5) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the read-write anti-dependency from `tx.apply_payment` (step 1) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.apply_payment` (step 2) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.apply_payment` (step 4) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the write-read from `tx.apply_payment` (step 4) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.apply_payment` (step 4) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-read from `tx.apply_payment` (step 5) to `tx.cancel_order` (step 1) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.apply_payment` (step 5) to `tx.cancel_order` (step 2) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.apply_payment` (step 5) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the read-write anti-dependency from `tx.apply_payment` (step 1) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.apply_payment` (step 2) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.apply_payment` (step 3) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the write-write from `tx.apply_payment` (step 3) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the read-write anti-dependency from `tx.apply_payment` (step 4) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.apply_payment` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the write-write from `tx.apply_payment` (step 4) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-write from `tx.apply_payment` (step 5) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the read-write anti-dependency from `tx.cancel_order` (step 1) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.cancel_order` (step 2) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.cancel_order` (step 3) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the write-read from `tx.cancel_order` (step 3) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.cancel_order` (step 3) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-read from `tx.cancel_order` (step 4) to `tx.apply_payment` (step 1) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.cancel_order` (step 4) to `tx.apply_payment` (step 2) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.cancel_order` (step 4) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the read-write anti-dependency from `tx.cancel_order` (step 1) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.cancel_order` (step 2) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.cancel_order` (step 3) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the write-read from `tx.cancel_order` (step 3) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.cancel_order` (step 3) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-read from `tx.cancel_order` (step 4) to `tx.cancel_order` (step 1) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.cancel_order` (step 4) to `tx.cancel_order` (step 2) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.cancel_order` (step 4) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the read-write anti-dependency from `tx.cancel_order` (step 1) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.cancel_order` (step 2) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the read-write anti-dependency from `tx.cancel_order` (step 3) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered by version validation: `tx.cancel_order` validates `object.order.version` at commit against the version it observed, and the conflicting mutation advances that version, so a stale observation rejects instead of committing",
        "  the write-write from `tx.cancel_order` (step 3) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-write from `tx.cancel_order` (step 4) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-read from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 1) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 2) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 3) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-read from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: `tx.apply_payment` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-write from `tx.create_order.new` (step 1) to `tx.apply_payment` (step 5) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-read from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 1) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 2) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-read from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: `tx.cancel_order` reads under read_committed isolation, so it observes only writes that had already committed",
        "  the write-write from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 3) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-write from `tx.create_order.new` (step 1) to `tx.cancel_order` (step 4) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  the write-write from `tx.create_order.new` (step 1) to `tx.create_order.new` (step 1) on `object.order` is commit-ordered: conflicting writes under declared isolation (read_committed and read_committed) are installed in commit order",
        "  no cyclic conflict component contains an unconstrained dependency: an apparent cycle would imply a cycle in strict commit order and cannot occur in a committed history",
        "the ordering key input.apply_payment.captured.order_id identifies the cursor domain: every identity field of object.order is pinned by it or by a literal",
        "the logical position input.apply_payment.captured.sequence is persisted as cursor object.order.last_applied_sequence under the successor rule at step 3: the transaction commits only when the position is admissible after the stored one, so an older accepted position cannot commit after a newer one",
        "the successor rule additionally makes accepted progression gap-free: a stale, duplicate, or skipped position rejects",
        "no ordinary write touches object.order.last_applied_sequence, and every advance of it uses the successor rule"
      ],
      "evidence": []
    },
    {
      "id": "oblig.operation.apply_payment.idempotency.0",
      "property": {
        "kind": "idempotency"
      },
      "subject": {
        "kind": "operation",
        "operation": "operation.apply_payment",
        "requirement": 0
      },
      "status": "proven",
      "summary": "Repeated attempts at operation.apply_payment sharing the declared key produce the effects of a single invocation.",
      "scope": "l0_only",
      "assumptions": [
        "on committed(tx.apply_payment): tx.apply_payment is committed on every attempt: tx.apply_payment commits are deduplicated by input.apply_payment.captured.event_id, so every attempt resolves the one commit",
        "on committed(tx.apply_payment): tx.apply_payment commits are deduplicated by input.apply_payment.captured.event_id, stable across the attempt class",
        "on committed(tx.apply_payment): duplicate executions of effect.order.paid publish the same logical message under topic.order_events's message identity (intent intent.apply_payment.order_paid is recovered from tx.apply_payment's keyed commit)",
        "on committed(tx.apply_payment): no modeled subscription on topic.order_events admits schema.OrderPaid; the cascade ends at the topic",
        "the identity of schema.PaymentCaptured on topic.order_events is carried by its producer's idempotency key (input.charge_payment.reserved.event_id, requirement #0): declared propagation from operation.charge_payment through effect.charge_payment.publish_captured"
      ],
      "evidence": [
        {
          "subject": "tx.apply_payment",
          "message": "the outcome of tx.apply_payment is not established to replay; a rejection commits nothing and performs only its rejection block's work, judged duplicate-safe on its own path, so divergence between rejection and commit cannot duplicate modeled work — the returned result is the result-replay obligation's concern."
        }
      ]
    },
    {
      "id": "oblig.operation.apply_payment.recoverability.0",
      "property": {
        "kind": "recoverability"
      },
      "subject": {
        "kind": "operation",
        "operation": "operation.apply_payment",
        "requirement": 0
      },
      "status": "proven",
      "summary": "An interrupted invocation of operation.apply_payment is re-driven until it reaches a terminal of its program.",
      "scope": "runtime_dependent",
      "assumptions": [
        "input.apply_payment.captured redelivers via topic.order_events at least once, re-driving interrupted invocations",
        "on committed(tx.apply_payment): tx.apply_payment resolves on re-encounter through its keyed commit (input.apply_payment.captured.event_id)",
        "on committed(tx.apply_payment): artifact intent.apply_payment.order_paid is recovered from tx.apply_payment's keyed commit on resumption"
      ],
      "evidence": []
    },
    {
      "id": "oblig.operation.charge_payment.idempotency.0",
      "property": {
        "kind": "idempotency"
      },
      "subject": {
        "kind": "operation",
        "operation": "operation.charge_payment",
        "requirement": 0
      },
      "status": "unknown",
      "summary": "Repeated attempts at operation.charge_payment sharing the declared key produce the effects of a single invocation.",
      "assumptions": [
        "the identity of schema.InventoryReserved on topic.order_events is carried by its producer's idempotency key (input.reserve_inventory.created.event_id, requirement #0): declared propagation from operation.reserve_inventory through effect.reserve_inventory.publish_reserved"
      ],
      "evidence": [
        {
          "subject": "effect.charge_payment.card",
          "message": "The path `ok(result.charge_payment.card)` applies external effect `effect.charge_payment.card`, declared `distinguishable`: a duplicate application may produce distinguishable modeled external work at that boundary."
        },
        {
          "message": "On the path `ok(result.charge_payment.card)`, the match on `result.charge_payment.card` at step `2`, taking its `ok` arm is not established to replay, so a retry may do different work: result `result.charge_payment.card` of `effect.charge_payment.card` is not replay-stable: the external boundary declares no terminal-result replay guarantee, so no same-identity terminal result is fixed."
        },
        {
          "subject": "effect.charge_payment.publish_failed",
          "message": "The instance `effect.charge_payment.publish_failed` constructs on the path `err:declined(result.charge_payment.card)` depends on roots that are not replay-stable: `reason` (effect result `result.charge_payment.card` of `effect.charge_payment.card` is not replay-stable: the external boundary declares no terminal-result replay guarantee, so no same-identity terminal result is fixed)."
        }
      ]
    },
    {
      "id": "oblig.operation.create_order.idempotency.0",
      "property": {
        "kind": "idempotency"
      },
      "subject": {
        "kind": "operation",
        "operation": "operation.create_order",
        "requirement": 0
      },
      "status": "unknown",
      "summary": "Repeated attempts at operation.create_order sharing the declared key produce the effects of a single invocation.",
      "assumptions": [],
      "evidence": [
        {
          "subject": "operation.reserve_inventory",
          "message": "The program publishes `schema.OrderCreated` to `topic.order_events` through `effect.create_order.publish_created`, and `operation.reserve_inventory` consumes it through `input.reserve_inventory.created`, whose idempotency requirement is not proven in this analysis, so the duplicate work a duplicate delivery causes there is not established to collapse."
        }
      ]
    },
    {
      "id": "oblig.operation.create_order.result_replay.0",
      "property": {
        "kind": "result_replay"
      },
      "subject": {
        "kind": "operation",
        "operation": "operation.create_order",
        "requirement": 0
      },
      "status": "proven",
      "summary": "Every attempt at operation.create_order sharing the declared key returns an equivalent result.",
      "scope": "l0_only",
      "assumptions": [
        "the returned ok payload is derived deterministically from output.create_order.order_id, output.create_order.status",
        "output.create_order is recovered from tx.create_order.new's keyed commit on every retry"
      ],
      "evidence": []
    },
    {
      "id": "oblig.operation.create_order.recoverability.0",
      "property": {
        "kind": "recoverability"
      },
      "subject": {
        "kind": "operation",
        "operation": "operation.create_order",
        "requirement": 0
      },
      "status": "proven",
      "summary": "An interrupted invocation of operation.create_order can be resumed to reach a terminal of its program.",
      "scope": "l0_only",
      "assumptions": [
        "tx.create_order.new resolves on re-encounter through its keyed commit (input.create_order.request.idempotency_key)",
        "artifact intent.create_order.publish_created is recovered from tx.create_order.new's keyed commit on resumption",
        "artifact output.create_order is recovered from tx.create_order.new's keyed commit on resumption"
      ],
      "evidence": []
    },
    {
      "id": "oblig.operation.reserve_inventory.tx.reserve_inventory.transaction_serializability.0",
      "property": {
        "kind": "transaction_serializability"
      },
      "subject": {
        "kind": "transaction",
        "operation": "operation.reserve_inventory",
        "transaction": "tx.reserve_inventory",
        "requirement": 0
      },
      "status": "unknown",
      "summary": "Executions of tx.reserve_inventory sharing key input.reserve_inventory.created.order_id commit in a history equivalent to some serial order, together with every transaction they may conflict with.",
      "remedy": "application",
      "assumptions": [],
      "evidence": [
        {
          "subject": "tx.reserve_inventory",
          "message": "The serializable-isolation route does not apply: 2 transactions in the conflict closure declare weaker isolation than serializable (`tx.reserve_inventory` is read_committed, `tx.transfer_stock` is read_committed). Every transaction that may conflict must be serializable for that route, since a serializable transaction is serializable only with respect to other serializable ones."
        },
        {
          "subject": "tx.reserve_inventory",
          "message": "A cyclic conflict component through `tx.reserve_inventory` → `tx.transfer_stock` → `tx.reserve_inventory` contains a dependency no declared fact commit-orders, so a non-serializable committed history through that chain cannot be excluded."
        },
        {
          "subject": "tx.reserve_inventory",
          "message": "Read-write anti-dependency: `tx.reserve_inventory` may read `object.stock` (step 1) before `tx.reserve_inventory` writes it (step 2), and neither strict locking nor version validation constrains the commit order — the anti-dependency behind write skew. Lock coverage is missing on the reader side: `tx.reserve_inventory` holds no shared or exclusive lock covering its step-1 access to `object.stock`. Lock coverage is missing on the writer side: `tx.reserve_inventory` holds no exclusive lock covering its step-2 access to `object.stock`. `tx.reserve_inventory` does not validate the version of `object.stock` it observed at commit, so a stale observation can still participate in a successful commit. The selected `object.stock` domains could not be proven disjoint, so the dependency is assumed (unknown selector overlap is never treated as disjoint)."
        },
        {
          "subject": "tx.reserve_inventory",
          "message": "Read-write anti-dependency: `tx.reserve_inventory` may read `object.stock` (step 1) before `tx.transfer_stock` writes it (step 4), and neither strict locking nor version validation constrains the commit order — the anti-dependency behind write skew. Lock coverage is missing on the reader side: `tx.reserve_inventory` holds no shared or exclusive lock covering its step-1 access to `object.stock`. `tx.reserve_inventory` does not validate the version of `object.stock` it observed at commit, so a stale observation can still participate in a successful commit. The selected `object.stock` domains could not be proven disjoint, so the dependency is assumed (unknown selector overlap is never treated as disjoint)."
        },
        {
          "subject": "tx.reserve_inventory",
          "message": "Read-write anti-dependency: `tx.reserve_inventory` may read `object.stock` (step 1) before `tx.transfer_stock` writes it (step 5), and neither strict locking nor version validation constrains the commit order — the anti-dependency behind write skew. Lock coverage is missing on the reader side: `tx.reserve_inventory` holds no shared or exclusive lock covering its step-1 access to `object.stock`. `tx.reserve_inventory` does not validate the version of `object.stock` it observed at commit, so a stale observation can still participate in a successful commit. The selected `object.stock` domains could not be proven disjoint, so the dependency is assumed (unknown selector overlap is never treated as disjoint)."
        }
      ]
    },
    {
      "id": "oblig.operation.reserve_inventory.idempotency.0",
      "property": {
        "kind": "idempotency"
      },
      "subject": {
        "kind": "operation",
        "operation": "operation.reserve_inventory",
        "requirement": 0
      },
      "status": "unknown",
      "summary": "Repeated attempts at operation.reserve_inventory sharing the declared key produce the effects of a single invocation.",
      "assumptions": [
        "the identity of schema.OrderCreated on topic.order_events is carried by its producer's idempotency key (input.create_order.request.idempotency_key, requirement #0): declared propagation from operation.create_order through effect.create_order.publish_created"
      ],
      "evidence": [
        {
          "subject": "tx.reserve_inventory",
          "message": "A duplicate attempt re-drives the program and re-encounters `tx.reserve_inventory`, which is retry-safe by neither route. Recovery: the transaction declares no keyed commit deduplication. Reconstruction: a mutation value depends on `reserved`, which is not replay-stable (it reaches transaction read `read.reserve_inventory.stock`, which is never replay-stable)."
        },
        {
          "subject": "operation.charge_payment",
          "message": "The program publishes `schema.InventoryReserved` to `topic.order_events` through `effect.reserve_inventory.publish_reserved`, and `operation.charge_payment` consumes it through `input.charge_payment.reserved`, whose idempotency requirement is not proven in this analysis, so the duplicate work a duplicate delivery causes there is not established to collapse."
        },
        {
          "subject": "tx.reserve_inventory",
          "message": "Intent `intent.reserve_inventory.publish_reserved` on the program is established by `tx.reserve_inventory` but replay-available through neither route, so a retry's instance may differ. Recovery: the transaction declares no keyed commit deduplication. Reconstruction: a mutation value depends on `reserved`, which is not replay-stable (it reaches transaction read `read.reserve_inventory.stock`, which is never replay-stable)."
        }
      ]
    },
    {
      "id": "oblig.operation.reserve_inventory.recoverability.0",
      "property": {
        "kind": "recoverability"
      },
      "subject": {
        "kind": "operation",
        "operation": "operation.reserve_inventory",
        "requirement": 0
      },
      "status": "unknown",
      "summary": "An interrupted invocation of operation.reserve_inventory is re-driven until it reaches a terminal of its program.",
      "assumptions": [],
      "evidence": [
        {
          "subject": "tx.reserve_inventory",
          "message": "Resuming the program re-encounters `tx.reserve_inventory` after it may have committed, but the commit resolves by neither route. Recovery: the transaction declares no keyed commit deduplication. Reconstruction: a mutation value depends on `reserved`, which is not replay-stable (it reaches transaction read `read.reserve_inventory.stock`, which is never replay-stable)."
        },
        {
          "subject": "tx.reserve_inventory",
          "message": "The program consumes artifact `intent.reserve_inventory.publish_reserved`, established by `tx.reserve_inventory`, but a resumption can supply it through neither route. Recovery: the transaction declares no keyed commit deduplication. Reconstruction: a mutation value depends on `reserved`, which is not replay-stable (it reaches transaction read `read.reserve_inventory.stock`, which is never replay-stable)."
        }
      ]
    }
  ]
}
